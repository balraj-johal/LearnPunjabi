/// <reference types="cypress" />
/**
 * can admin create new lesson
 * can admin update lesson info
 * can admin add new tasks of each type
 * can admin save lesson
 * can admin then test that lesson
 * can admin edit that lesson
 */