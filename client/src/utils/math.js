/**
 * Generates cryptographically secure random string of specified length
 * @name genVerificationCode
 * @param  {Number} length
 * @returns {String} code
*/
let degreesToRads = (degrees) => {
    return degrees * (Math.PI / 180);
}

export { degreesToRads };